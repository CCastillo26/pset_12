# simulate_arrival_times()
# Input: lambda(t), lambda_max
# Output: proposed arrival times t_i

source("estimation.R")

simulate_arrival_times() <- function() {
  ti <- bike_df %>%
    
}

# simulate_days()
# Input: thinned arrival times
# Output: inventory by station

simulate_days() <- function() {
  
}
